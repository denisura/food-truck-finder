export const filterSelector = state => state.meta.filter;
export const orderSelector = state => state.meta.order;
export const paginationSelector = state => state.meta.pagination;
export const hasMoreSelector = state => state.meta.pagination.hasMore;
export const statusSelector = state => state.meta.status;
export const metaSelector = state => state.meta;
